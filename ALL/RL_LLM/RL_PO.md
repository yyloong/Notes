# RL Policy Optimization — Algorithm Comparison

| Intuition | Algorithm | Advantage $\hat{A}$ | IS Ratio $s(\theta)$ | Loss Function |
|-----------|-----------|---------------------|----------------------|---------------|
| ***🔹 Token-level Action*** | | | | |
| TODO | **PPO** | $\hat{A}_{i,t}^{\text{GAE}(\gamma,\lambda)}=\displaystyle\sum_{l=0}^{\infty}(\lambda\gamma)^{l}\delta_{i,t+l}$ <br><br> $\delta_{i,l}=r_{i,l}+\gamma V(y_{i,\leq l+1})-V(y_{i,\leq l})$ | $s_{i,t}(\theta)=\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}$ | $\mathbb{E}_{x\sim\mathcal{D},\,y\sim\pi_{\text{old}}}\!\Big[\min\!\big(s_{i,t}(\theta)\hat{A}_{i,t},\;\text{clip}(s_{i,t}(\theta),1{-}\epsilon,1{+}\epsilon)\hat{A}_{i,t}\big)\Big]$ |
| TODO | **GRPO** | $\hat{A}_{i,t}=\dfrac{r_{i,t}-\text{mean}(\{r_{i,t}\}_{i=1}^{G})}{\text{std}(\{r_{i,t}\}_{i=1}^{G})},\quad r_{i,t}=R(x,y_{i,t})$ | $s_{i,t}(\theta)=\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}$ | $\mathbb{E}_{x\sim\mathcal{D},\,y\sim\pi_{\text{old}}}\!\left[\dfrac{1}{G}\displaystyle\sum_{i=1}^{G}\dfrac{1}{\lvert y_i\rvert}\sum_{t=1}^{\lvert y_i\rvert}\min\!\big(s_{i,t}(\theta)\hat{A}_{i,t},\;\text{clip}(s_{i,t}(\theta),1{-}\epsilon,1{+}\epsilon)\hat{A}_{i,t}\big)\right]$ |
| TODO | **DAPO** | $\hat{A}_{i,t}=\dfrac{r_{i,t}-\text{mean}(\{r_{i,t}\}_{i=1}^{G})}{\text{std}(\{r_{i,t}\}_{i=1}^{G})}$ | $s_{i,t}(\theta)=\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}$ | $\mathbb{E}_{x\sim\mathcal{D},\,y\sim\pi_{\text{old}}}\!\left[\dfrac{1}{\sum_{i=1}^{G}\lvert y_i\rvert}\displaystyle\sum_{i=1}^{G}\sum_{t=1}^{\lvert y_i\rvert}\min\!\big(s_{i,t}(\theta)\hat{A}_{i,t},\;\text{clip}(s_{i,t}(\theta),1{-}\epsilon_{\text{low}},1{+}\epsilon_{\text{high}})\hat{A}_{i,t}\big)\right]$ <br><br> $\text{s.t.}\quad 0<\lvert\{y_i\mid\text{is\_equiv}(y_i,\text{ans}_i)\}\rvert<G$ |
| TODO | **FIPO** | $\Delta\log p_k=\log\dfrac{\pi_{\theta}(y_k\mid x,y_{{<}k})}{\pi_{\text{old}}(y_k\mid x,y_{{<}k})}$ <br><br> $\text{FKL}_t=\displaystyle\sum_{k=t}^{T}\mathbb{I}\!\left(\dfrac{\pi_{\theta}(y_k\mid x,y_{{<}k})}{\pi_{\text{old}}(y_k\mid x,y_{{<}k})}\leq c\right)\gamma^{k-t}\Delta\log p_k$ <br><br> $\hat{A}_{i,t}=\dfrac{r_{i,t}-\text{mean}(\{r_{i,t}\}_{i=1}^G)}{\text{std}(\{r_{i,t}\}_{i=1}^G)}\cdot\mathrm{clip}\!\left(\exp(\text{FKL}_{i,t}),1-\epsilon_{f_{\text{low}}},1+\epsilon_{f_{\text{high}}}\right)$ | $s_{i,t}(\theta)=\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}$ | $\mathbb{E}_{x\sim\mathcal{D},\,y\sim\pi_{\text{old}}}\!\left[\dfrac{1}{\sum_{i=1}^{G}\lvert y_i\rvert}\displaystyle\sum_{i=1}^{G}\sum_{t=1}^{\lvert y_i\rvert}\min\!\big(s_{i,t}(\theta)\hat{A}_{i,t},\;\text{clip}(s_{i,t}(\theta),1{-}\epsilon_{\text{low}},1{+}\epsilon_{\text{high}})\hat{A}_{i,t}\big)\right]$ |
| ***🔷 Sequence / Step-level Action*** | | | | |
| TODO | **GSPO** | $\hat{A}_{i}=\dfrac{r_{i}-\text{mean}(\{r_{i}\}_{i=1}^{G})}{\text{std}(\{r_{i}\}_{i=1}^{G})},\quad r_i=R(x,y_i)$ | $s_{i}(\theta)=\!\left(\dfrac{\pi_{\theta}(y_{i}\mid x)}{\pi_{\theta_{\text{old}}}(y_{i}\mid x)}\right)^{\!\frac{1}{\lvert y_i\rvert}}=\exp\!\left(\dfrac{1}{\lvert y_i\rvert}\sum_{t=1}^{\lvert y_i\rvert}\log\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}\right)$ | $\mathbb{E}_{x\sim\mathcal{D},\,y\sim\pi_{\text{old}}}\!\left[\dfrac{1}{G}\displaystyle\sum_{i=1}^{G}\min\!\big(s_{i}(\theta)\hat{A}_{i},\;\text{clip}(s_{i}(\theta),1{-}\epsilon,1{+}\epsilon)\hat{A}_{i}\big)\right]$ |
| TODO | **GiGPO** | *(此处 $t$ 为序列步级，非 token 级)* <br> $\hat{A}_{i,t}=A^E_i+\omega\cdot A^S_{i,t}$ <br><br> $A^E_i=\dfrac{r_i-\text{mean}(\{r_i\}_{i=1}^{N})}{\text{std}(\{r_i\}_{i=1}^{N})},\quad r_i=R(x,y_i)$ <br><br> $G^S(\bar{s})=\{(y_{j,t'},r_{j,t'})\mid s_{j,t'}=\bar{s},\;1\leq j\leq N,\;1\leq t'\leq T\}$ <br><br> $A^S_{i,t}=\dfrac{r_{i,t}-\text{mean}(\{r\mid(\cdot,r)\in G^S(s_{i,t})\})}{F_{\text{norm}}(\{r\mid(\cdot,r)\in G^S(s_{i,t})\})}$ | $s_{i,t}(\theta)=\dfrac{\pi_{\theta}(y_{i,t}\mid x,\,y_{i,{<}t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,\,y_{i,{<}t})}$ | $\mathbb{E}_{x\sim p(X),\,\{\tau_i\}\sim\pi_{\text{old}}}\!\left[\dfrac{1}{NT}\displaystyle\sum_{i=1}^{N}\sum_{t=1}^{T}\min\!\big(s_{i,t}(\theta)\hat{A}_{i,t},\;\text{clip}(s_{i,t}(\theta),1{-}\epsilon,1{+}\epsilon)\hat{A}_{i,t}\big)\right]$ |

---

> **公式校注**
>
> - **PPO \[已修正①\]**：GAE 指数 $(\lambda\gamma)^{l+t}$ → $(\lambda\gamma)^{l}$，求和哑变量为 $l$，$t$ 为当前时间步。
> - **PPO \[已修正②\]**：TD 误差中 $V(y_{i,\leq l+t})$ → $V(y_{i,\leq l+1})$（下一个 token 位置）。
> - **FIPO \[已修正\]**：$\mathbb{I}(\cdot)$ 内条件改为 IS 比值 $\leq c$（截断阈值）；$\hat{A}_{i,t}$ 乘以 $\mathrm{clip}(\exp(\text{FKL}_{i,t}),\cdot)$ 作为 future-aware 加权因子。
> - **渲染修正**：所有绝对值 $\lvert y_i\rvert$ 改用 `\lvert...\rvert`（避免裸 `|` 被 Markdown 解析为列分隔符）；条件概率中的 $y_{i,{<}t}$ 用 `{<}` 包裹防止被识别为 HTML 标签。
> - **GRPO/DAPO \[说明\]**：奖励统一为序列级 $r_i = R(x,y_i)$，与原论文一致。
> - **GSPO \[说明\]**：IS ratio 取序列联合概率比的 $\frac{1}{\lvert y_i\rvert}$ 次方，实现长度归一化。
> - **GiGPO \[说明\]**：双级优势：$A^E_i$ 为 episode 级 group-relative 优势；$A^S_{i,t}$ 的比较集合 $G^S(s_{i,t})$ 收集**所有轨迹 $j$、所有时间步 $t'$** 中到达同一状态 $s_{i,t}$ 的 (action, reward) 对，而非只比较同一 $t$ 时刻——这是 GiGPO 的核心设计。**注意**：GiGPO 中的 $t$ 是 **序列步（reasoning step/turn）级别**，与其他算法中 token 级别的 $t$ 含义不同。
